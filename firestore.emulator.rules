rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // RÈGLES POUR L'ÉMULATEUR - ACCÈS TOTAL
    // ⚠️ NE PAS UTILISER EN PRODUCTION !
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
